<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.member">
    <resultMap id="memResult" type="memberDTO">
        <result property="userId" column="USER_ID" />
        <result property="userPw" column="USER_PW" />
        <result property="userNickname" column="USER_NICKNAME" />
        <result property="userName" column="USER_NAME" />
        <result property="userEmail" column="USER_EMAIL" />
        <result property="userBirthday" column="USER_BIRTHDAY" />
        <result property="userPhoneNumber" column="USER_PHONENUMBER" />
    </resultMap>

    <!-- 회원 추가 -->
    <insert id="addMember" parameterType="MemberDTO">
        <![CDATA[
        insert into users (ID, USER_ID, USER_PW, USER_NICKNAME, USER_NAME, USER_EMAIL, USER_BIRTHDAY, USER_PHONENUMBER, USER_JOINDATE)
        values (USER_SEQ.NEXTVAL, #{userId, jdbcType=VARCHAR}, #{userPw, jdbcType=VARCHAR}, #{userNickname, jdbcType=VARCHAR}, #{userName, jdbcType=VARCHAR}, #{userEmail, jdbcType=VARCHAR}, #{userBirthday, jdbcType=VARCHAR}, #{userPhoneNumber, jdbcType=VARCHAR}, sysdate)
        ]]>
    </insert>

    <!-- 아이디 중복 확인 -->
    <select id="checkId" parameterType="String" resultType="int">
        SELECT COUNT(*) FROM users WHERE USER_ID = #{userId}
    </select>

    <!-- 닉네임 중복 확인 -->
    <select id="checkNickname" parameterType="String" resultType="int">
        SELECT COUNT(*) FROM users WHERE USER_NICKNAME = #{userNickname}
    </select>

    <select id="getUserById" parameterType="String" resultMap="memResult">
        SELECT * FROM users WHERE USER_ID = #{userId}
    </select>

    <select id="checkPassword" parameterType="Map" resultType="int">
        SELECT COUNT(*) FROM users WHERE USER_ID = #{userId} AND USER_PW = #{userPw}
    </select>


</mapper>